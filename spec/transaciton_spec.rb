require "spec_helper"

describe VendingMachine::Transaction do
  xit "initializes" do
    
  end
  
  it "lets a product to be selected"
  it "lets a coin to be inserted in"
  
  # after each event -> callback
  #  returns the product / asks for more coins (reopen a transaction block)
end

